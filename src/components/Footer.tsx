import { ArrowUp, ArrowUpRight } from 'lucide-react';
import { Brand } from './Brand';

export function Footer({ onNavigate, onJourney, onAbout, onPrivacy }: { onNavigate: (page: 'discover' | 'explore' | 'how-it-works') => void; onJourney: () => void; onAbout: () => void; onPrivacy: () => void }) {
  return <footer className="site-footer"><div className="container"><div className="footer-main"><div className="footer-brand"><a href="#discover" onClick={(event) => { event.preventDefault(); onNavigate('discover'); }} aria-label="SoniX home"><Brand /></a><p>A new angle on being human.</p></div><div className="footer-links"><div><button onClick={() => onNavigate('discover')}>Discover</button><button onClick={() => onNavigate('explore')}>Explore situations</button><button onClick={onJourney}>My journey <ArrowUpRight size={13} /></button></div><div><button onClick={() => onNavigate('how-it-works')}>How it works</button><button onClick={onAbout}>About the demo</button><button onClick={onPrivacy}>Privacy & your data</button></div></div><button className="back-to-top" onClick={() => window.scrollTo({ top: 0, behavior: window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth' })} aria-label="Back to top"><ArrowUp size={18} /></button></div><div className="footer-bottom"><span>&copy; {new Date().getFullYear()} SoniX. A little more perspective.</span><span>Made for curious minds. Not algorithms.</span></div></div></footer>;
}